{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {},
    "release": {
      "dependsOn": ["^release"],
      "cache": false,
      "interactive": true
    }
  }
}

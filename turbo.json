{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {},
    "tsc": {},
    "release": {
      "dependsOn": ["^release"],
      "cache": false
    }
  }
}
